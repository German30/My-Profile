import { HoverAnimationDirective } from './hover-animation.directive';

describe('HoverAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
